<!-- Mobile Header -->
<header class="mobile-header">
  <span class="mobile-logo"><%= t('book.childs_book', { name: book.child_first_name }) %></span>
  <button class="hamburger" onclick="toggleNav()" aria-label="Menu">
    <span></span><span></span><span></span>
  </button>
</header>

<!-- Nav Overlay -->
<div class="nav-overlay" id="navOverlay" onclick="toggleNav()"></div>

<!-- Sidebar Navigation -->
<nav id="sidebar">
  <div class="sidebar-header">
    <div class="sidebar-logo">&#10022; Legacy Odyssey</div>
    <div class="sidebar-child-name"><%= book.child_first_name %><%= book.child_middle_name ? ' ' + book.child_middle_name : '' %> <%= book.child_last_name %></div>
    <div class="sidebar-tagline"><%= t('book.born') %> &middot; <%= book.birth_date ? new Date(book.birth_date).toLocaleDateString(({ es: 'es-ES', hi: 'hi-IN' }[lang] || 'en-US'), { month: 'long', day: 'numeric', year: 'numeric' }) : '' %> &middot; <%= book.city || '' %></div>
    <div class="lang-toggle" style="margin-top:0.7rem;display:inline-flex;gap:2px;border:1px solid rgba(200,169,110,0.4);border-radius:999px;padding:2px;font-family:'Jost',sans-serif;font-size:0.7rem;letter-spacing:0.05em;">
      <% var _LL = { en: 'EN', es: 'ES', hi: 'हिं' }; (typeof supportedLangs !== 'undefined' ? supportedLangs : ['en','es']).forEach(function(L){ %>
        <a href="?lang=<%= L %>" style="padding:2px 11px;border-radius:999px;text-decoration:none;<%= lang === L ? 'background:#c8a96e;color:#1a1510;font-weight:600;' : 'color:#8a7e6b;' %>"><%= _LL[L] || L.toUpperCase() %></a>
      <% }); %>
    </div>
  </div>
  <div class="sidebar-nav">
    <div class="nav-section-label"><%= t('nav.the_book') %></div>
    <a class="nav-item active" onclick="showPage('welcome')">
      <span class="nav-icon"><i data-lucide="sparkles"></i></span>
      <span class="nav-label"><%= t('nav.welcome') %></span>
    </a>
    <% if (visibleSections.before) { %>
    <a class="nav-item" onclick="showPage('before')">
      <span class="nav-icon"><i data-lucide="heart"></i></span>
      <span class="nav-label"><%= t('nav.before') %></span>
    </a>
    <% } %>
    <% if (visibleSections.birth) { %>
    <a class="nav-item" onclick="showPage('birth')">
      <span class="nav-icon"><i data-lucide="book-open"></i></span>
      <span class="nav-label"><%= t('nav.birth_story') %></span>
    </a>
    <% } %>
    <% if (visibleSections.birthday) { %>
    <a class="nav-item" onclick="showPage('birthday')">
      <span class="nav-icon"><i data-lucide="camera"></i></span>
      <span class="nav-label"><%= t('nav.birth_day') %></span>
    </a>
    <% } %>
    <% if (visibleSections.journey) { %>
    <a class="nav-item" onclick="showPage('journey')">
      <span class="nav-icon"><i data-lucide="route"></i></span>
      <span class="nav-label"><%= t('nav.journey') %></span>
    </a>
    <% } %>

    <% if (isFree) { %>
    <!-- Locked sections shown in sidebar for free users -->
    <a class="nav-item" onclick="showPage('home')" style="opacity:0.5;">
      <span class="nav-icon"><i data-lucide="lock"></i></span>
      <span class="nav-label"><%= t('nav.coming_home') %></span>
    </a>
    <a class="nav-item" onclick="showPage('home')" style="opacity:0.5;">
      <span class="nav-icon"><i data-lucide="lock"></i></span>
      <span class="nav-label"><%= t('nav.month_by_month') %></span>
    </a>
    <div class="nav-section-label"><%= t('nav.family_memories') %></div>
    <a class="nav-item" onclick="showPage('home')" style="opacity:0.5;">
      <span class="nav-icon"><i data-lucide="lock"></i></span>
      <span class="nav-label"><%= t('nav.our_family') %></span>
    </a>
    <a class="nav-item" onclick="showPage('home')" style="opacity:0.5;">
      <span class="nav-icon"><i data-lucide="lock"></i></span>
      <span class="nav-label"><%= t('nav.your_firsts') %></span>
    </a>
    <a class="nav-item" onclick="showPage('home')" style="opacity:0.5;">
      <span class="nav-icon"><i data-lucide="lock"></i></span>
      <span class="nav-label"><%= t('nav.celebrations') %></span>
    </a>
    <a class="nav-item" onclick="showPage('home')" style="opacity:0.5;">
      <span class="nav-icon"><i data-lucide="lock"></i></span>
      <span class="nav-label"><%= t('nav.letters') %></span>
    </a>
    <a class="nav-item" onclick="showPage('home')" style="opacity:0.5;">
      <span class="nav-icon"><i data-lucide="lock"></i></span>
      <span class="nav-label"><%= t('nav.recipes') %></span>
    </a>
    <a class="nav-item" onclick="showPage('home')" style="opacity:0.5;">
      <span class="nav-icon"><i data-lucide="lock"></i></span>
      <span class="nav-label"><%= t('nav.keepsakes') %></span>
    </a>
    <% } else { %>
    <% if (visibleSections.home) { %>
    <a class="nav-item" onclick="showPage('home')">
      <span class="nav-icon"><i data-lucide="home"></i></span>
      <span class="nav-label"><%= t('nav.coming_home') %></span>
    </a>
    <% } %>
    <% if (visibleSections.months) { %>
    <a class="nav-item" onclick="showPage('months')">
      <span class="nav-icon"><i data-lucide="calendar"></i></span>
      <span class="nav-label"><%= t('nav.month_by_month') %></span>
    </a>
    <% } %>
    <% if (visibleSections.family || visibleSections.moments || visibleSections.firsts || visibleSections.holidays || visibleSections.letters || visibleSections.recipes) { %>
    <div class="nav-section-label"><%= t('nav.family_memories') %></div>
    <% } %>
    <% if (visibleSections.family) { %>
    <a class="nav-item" onclick="showPage('family')">
      <span class="nav-icon"><i data-lucide="users"></i></span>
      <span class="nav-label"><%= t('nav.our_family') %></span>
    </a>
    <% } %>
    <% if (visibleSections.moments) { %>
    <a class="nav-item" onclick="showPage('moments')">
      <span class="nav-icon"><i data-lucide="video"></i></span>
      <span class="nav-label"><%= t('nav.video_moments') %></span>
    </a>
    <% } %>
    <% if (visibleSections.galleries) { %>
    <%
      // Photo Galleries submenu — same nested-dropdown UX as Celebrations.
      // Lists each gallery that actually has photos; clicking one opens the
      // galleries page and scrolls to that gallery's block (#cg-<id>).
      // Detail layouts (celebration/recipe) don't pass customGalleries, so
      // guard and fall back to a plain nav item there.
      const _galsSrc = (typeof customGalleries !== 'undefined' && customGalleries) ? customGalleries : [];
      const _galsWithPhotos = _galsSrc.filter((g) => g.photos && g.photos.length);
    %>
    <% if (_galsWithPhotos.length) { %>
    <div class="nav-parent" onclick="this.classList.toggle('open')">
      <span class="nav-icon"><i data-lucide="images"></i></span>
      <a class="nav-label-link" onclick="event.stopPropagation();showPage('galleries');if(window.showGalleryIndex)showGalleryIndex();" style="flex:1;color:inherit;text-decoration:none;"><%= t('nav.photo_galleries') %></a>
      <span class="nav-chevron">▸</span>
    </div>
    <div class="nav-children">
      <% _galsWithPhotos.forEach(function(g) { %>
        <a class="nav-celebration" onclick="showPage('galleries');if(window.showGallery)showGallery('<%= g.id %>');"><%= g.title || 'Untitled Gallery' %></a>
      <% }); %>
    </div>
    <% } else { %>
    <a class="nav-item" onclick="showPage('galleries')">
      <span class="nav-icon"><i data-lucide="images"></i></span>
      <span class="nav-label"><%= t('nav.photo_galleries') %></span>
    </a>
    <% } %>
    <% } %>
    <% if (visibleSections.firsts) { %>
    <a class="nav-item" onclick="showPage('firsts')">
      <span class="nav-icon"><i data-lucide="star"></i></span>
      <span class="nav-label"><%= t('nav.your_firsts') %></span>
    </a>
    <% } %>
    <% if (visibleSections.holidays) { %>
    <%
      // Build the nested celebration menu. Years come from book.celebration_years
      // (or default to 'Your First Year'). For each year, list the celebrations
      // that actually have content (title, body, location, attendees, gifts, or
      // photos) — empty placeholder rows are skipped so the menu doesn't get
      // cluttered with "(untitled)" entries.
      const celebrationYears = (typeof celebrationsByYear !== 'undefined' && celebrationsByYear)
        ? celebrationsByYear
        : [];
      const hasMeaningful = (c) => c && (
        (c.title && c.title.trim() && c.title !== '(untitled)') ||
        (c.body && c.body.trim()) ||
        c.location || c.attendees || c.gifts ||
        (c.photos && c.photos.length > 0) || c.photo_path
      );
      function slugifyYearLabel(label) {
        if (!label) return 'year';
        const s = String(label).toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '');
        return s || 'year';
      }
      function celebSlug(c) {
        if (c && c.slug) return c.slug;
        const base = (c && c.title) ? c.title : 'celebration';
        return base.toString().toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '') || 'celebration';
      }
      // Auto-open the year containing the currently-viewed celebration (if any),
      // and auto-open the parent Celebrations menu.
      const activeCelebrationId = (typeof activeCelebration !== 'undefined' && activeCelebration) ? activeCelebration.id : null;
      const activeYearLabel = (typeof activeCelebration !== 'undefined' && activeCelebration) ? activeCelebration.year_label : null;
      const isOnCelebrationDetail = !!activeCelebrationId;
    %>
    <div class="nav-parent <%= isOnCelebrationDetail ? 'open active' : '' %>"
         onclick="this.classList.toggle('open')">
      <span class="nav-icon"><i data-lucide="gift"></i></span>
      <a class="nav-label-link" onclick="event.stopPropagation();showPage('holidays')" style="flex:1;color:inherit;text-decoration:none;"><%= t('nav.celebrations') %></a>
      <span class="nav-chevron">▸</span>
    </div>
    <div class="nav-children">
      <%
        // Visitor-side sidebar only shows years that have at least one
        // celebration with actual content. Empty placeholder years (like the
        // auto-created "Year 2" / "Year 3" rows from the mobile editor) are
        // hidden from visitors. The account-dashboard editor still shows
        // every year so the owner can add celebrations into them.
        const yearsWithContent = celebrationYears.filter((y) => (y.items || []).some(hasMeaningful));
        const onlyOneYearWithContent = yearsWithContent.length === 1;
      %>
      <% yearsWithContent.forEach(function(year) { %>
        <% const yearSlug = slugifyYearLabel(year.label); %>
        <% const isActiveYear = (activeYearLabel === year.label) || onlyOneYearWithContent; %>
        <div class="nav-year <%= isActiveYear ? 'open' : '' %>"
             onclick="event.stopPropagation();this.classList.toggle('open')">
          <span><%= year.label %></span>
          <span class="nav-year-chevron">▸</span>
        </div>
        <div class="nav-celebrations">
          <% year.items.filter(hasMeaningful).forEach(function(c) { %>
            <a class="nav-celebration <%= activeCelebrationId === c.id ? 'active' : '' %>"
               href="/celebrations/<%= yearSlug %>/<%= celebSlug(c) %>"><%= c.title %></a>
          <% }); %>
        </div>
      <% }); %>
    </div>
    <% } %>
    <% if (visibleSections.letters) { %>
    <a class="nav-item" onclick="showPage('letters')">
      <span class="nav-icon"><i data-lucide="mail"></i></span>
      <span class="nav-label"><%= t('nav.letters') %></span>
    </a>
    <% } %>
    <% if (visibleSections.recipes) { %>
    <%
      // Build the recipes submenu — same nested-dropdown UX as celebrations.
      // We accept either `recipes` (from book.ejs SPA) or `recipesList`
      // (from layouts/recipe-detail.ejs) — both flat arrays of recipe rows.
      const _recipesSrc = (typeof recipesList !== 'undefined' && recipesList)
        ? recipesList
        : ((typeof recipes !== 'undefined' && recipes) ? recipes : []);
      const _hasMeaningfulRecipe = (r) => r && (
        (r.title && r.title.trim() && r.title !== '(untitled)' && r.title !== 'New Recipe') ||
        (r.description && r.description.trim()) ||
        (r.story && r.story.trim()) ||
        (r.ingredients && r.ingredients.length > 0) ||
        (r.directions && r.directions.length > 0) ||
        (r.photos && r.photos.length > 0) ||
        r.photo_path
      );
      const _visibleRecipes = _recipesSrc.filter(_hasMeaningfulRecipe);
      const _activeRecipeId = (typeof activeRecipe !== 'undefined' && activeRecipe) ? activeRecipe.id : null;
      const _isOnRecipeDetail = !!_activeRecipeId;
      function _recipeSlugOf(x) {
        if (x && x.slug) return x.slug;
        const base = (x && x.title) ? x.title : 'recipe';
        return base.toString().toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '') || 'recipe';
      }
    %>
    <div class="nav-parent <%= _isOnRecipeDetail ? 'open active' : '' %>"
         onclick="this.classList.toggle('open')">
      <span class="nav-icon"><i data-lucide="utensils-crossed"></i></span>
      <a class="nav-label-link" onclick="event.stopPropagation();showPage('recipes')" style="flex:1;color:inherit;text-decoration:none;"><%= t('nav.recipes') %></a>
      <span class="nav-chevron">▸</span>
    </div>
    <div class="nav-children">
      <% _visibleRecipes.forEach(function(r) { %>
        <a class="nav-celebration <%= _activeRecipeId === r.id ? 'active' : '' %>"
           href="/recipes/<%= _recipeSlugOf(r) %>"><%= r.title %></a>
      <% }); %>
    </div>
    <% } %>
    <% if (visibleSections.keepsakes) { %>
    <%
      // Keepsakes submenu — same nested-dropdown UX as recipes/celebrations.
      const _keepsakesSrc = (typeof keepsakesList !== 'undefined' && keepsakesList)
        ? keepsakesList
        : ((typeof keepsakes !== 'undefined' && keepsakes) ? keepsakes : []);
      const _hasMeaningfulKeepsake = (k) => k && (
        (k.title && k.title.trim() && k.title !== '(untitled)' && k.title !== 'New Keepsake') ||
        (k.description && k.description.trim()) ||
        (k.story && k.story.trim()) ||
        (k.photos && k.photos.length > 0)
      );
      const _visibleKeepsakes = _keepsakesSrc.filter(_hasMeaningfulKeepsake);
      const _activeKeepsakeId = (typeof activeKeepsake !== 'undefined' && activeKeepsake) ? activeKeepsake.id : null;
      const _isOnKeepsakeDetail = !!_activeKeepsakeId;
      function _keepsakeSlugOf(x) {
        if (x && x.slug) return x.slug;
        const base = (x && x.title) ? x.title : 'keepsake';
        return base.toString().toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '') || 'keepsake';
      }
    %>
    <div class="nav-parent <%= _isOnKeepsakeDetail ? 'open active' : '' %>"
         onclick="this.classList.toggle('open')">
      <span class="nav-icon"><i data-lucide="archive"></i></span>
      <a class="nav-label-link" onclick="event.stopPropagation();showPage('keepsakes')" style="flex:1;color:inherit;text-decoration:none;"><%= t('nav.keepsakes') %></a>
      <span class="nav-chevron">▸</span>
    </div>
    <div class="nav-children">
      <% _visibleKeepsakes.forEach(function(k) { %>
        <a class="nav-celebration <%= _activeKeepsakeId === k.id ? 'active' : '' %>"
           href="/keepsakes/<%= _keepsakeSlugOf(k) %>"><%= k.title %></a>
      <% }); %>
    </div>
    <% } %>
    <% if (visibleSections.vault) { %>
    <div class="nav-section-label"><%= t('nav.the_vault') %></div>
    <a class="nav-item locked" onclick="showPage('vault')">
      <span class="nav-icon"><i data-lucide="lock"></i></span>
      <span class="nav-label"><%= t('nav.locked_until_18') %></span>
    </a>
    <% } %>
    <% } %>
  </div>
  <div class="sidebar-footer">
    <% if (isDemoDomain) { %>
    <p style="color:#d4bb8a;font-size:0.78rem;line-height:1.5;"><%= t('footer.demo_line') %><br><%= t('footer.create_own') %></p>
    <a href="https://www.legacyodyssey.com" target="_blank" class="sidebar-edit-link" style="background:#c8a96e;color:#1a1510;padding:0.4rem 0.75rem;border-radius:2px;font-weight:600;text-align:center;display:block;">
      <%= t('footer.get_your_own') %>
    </a>
    <% } else if (isFree) { %>
    <p style="color:#c8a96e;font-weight:600;"><%= t('footer.free_plan') %></p>
    <a href="https://legacyodyssey.com/#pricing" target="_blank" class="sidebar-edit-link" style="background:#c8a96e;color:#1a1510;padding:0.4rem 0.75rem;border-radius:2px;font-weight:600;">
      <%= t('footer.upgrade') %>
    </a>
    <% } else { %>
    <p><%= t('footer.crafted_by') %><br><span>Legacy Odyssey</span><br>legacyodyssey.com</p>
    <a href="https://legacyodyssey.com/account" class="sidebar-edit-link" target="_blank" rel="noopener">
      <%= t('footer.edit_my_book') %>
    </a>
    <% } %>
  </div>
</nav>
