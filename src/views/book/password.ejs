<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%- family.display_name || 'Baby Book' %> — A Legacy Odyssey</title>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,600;1,300;1,400&family=Jost:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'Jost', sans-serif; background: #1a1510; color: #f5f0eb; min-height: 100vh; display: flex; align-items: center; justify-content: center; }
    .container { text-align: center; max-width: 500px; padding: 2rem; }
    .brand { font-family: 'Jost', sans-serif; font-size: 0.75rem; letter-spacing: 0.3em; text-transform: uppercase; color: #c8a96e; margin-bottom: 1rem; }
    h1 { font-family: 'Cormorant Garamond', serif; font-size: 3rem; font-weight: 300; line-height: 1.2; margin-bottom: 0.5rem; }
    h1 em { color: #c8a96e; font-style: italic; }
    .subtitle { font-size: 0.7rem; letter-spacing: 0.25em; text-transform: uppercase; color: #a09080; margin-bottom: 2rem; }
    .divider { width: 40px; height: 2px; background: #c8a96e; margin: 1.5rem auto; }
    label { display: block; font-size: 0.65rem; letter-spacing: 0.2em; text-transform: uppercase; color: #a09080; margin-bottom: 0.5rem; }
    input[type="password"] { width: 100%; padding: 0.9rem; background: rgba(255,255,255,0.08); border: 1px solid rgba(200,169,110,0.3); color: #f5f0eb; font-family: 'Jost', sans-serif; font-size: 1rem; text-align: center; letter-spacing: 0.1em; outline: none; }
    input[type="password"]:focus { border-color: #c8a96e; }
    button { width: 100%; padding: 0.9rem; background: transparent; border: 1px solid #c8a96e; color: #c8a96e; font-family: 'Jost', sans-serif; font-size: 0.75rem; letter-spacing: 0.25em; text-transform: uppercase; cursor: pointer; margin-top: 0.75rem; transition: all 0.3s; }
    button:hover { background: #c8a96e; color: #1a1510; }
    .error { color: #e74c3c; font-size: 0.8rem; margin-top: 0.75rem; }
    .hint { color: #706050; font-size: 0.75rem; margin-top: 1.5rem; }
    .hint strong { color: #a09080; }
  </style>
</head>
<body>
  <div class="container">
    <div class="brand">✦ Legacy Odyssey ✦</div>
    <h1><em><%- family.display_name || "Your Child's" %></em></h1>
    <div class="subtitle">A Living Baby Book & Family Heirloom</div>
    <div class="divider"></div>
    <form method="POST" action="/verify-password">
      <input type="hidden" name="slug" value="<%- family.subdomain %>">
      <label>Enter the Family Password to Continue</label>
      <input type="password" name="password" placeholder="••••••••" autofocus>
      <button type="submit">Open Book →</button>
      <%- error ? '<div class="error">Incorrect password. Please try again.</div>' : '' %>
    </form>
    <div class="hint">Demo password: <strong>legacy</strong></div>
  </div>
</body>
</html>
