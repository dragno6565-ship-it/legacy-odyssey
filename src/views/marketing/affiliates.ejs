<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Rewardful affiliate tracking (Friends of Legacy Odyssey) -->
  <script>(function(w,r){w._rwq=r;w[r]=w[r]||function(){(w[r].q=w[r].q||[]).push(arguments)}})(window,'rewardful');</script>
  <script async src='https://r.wdfl.co/rw.js' data-rewardful='0a0312'></script>

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Affiliate Program — Earn 35% Recurring | Legacy Odyssey</title>
  <meta name="description" content="Earn 35% recurring commission for every parent you refer to Legacy Odyssey — a digital baby book at your child's own .com domain. 90-day cookie, $50 minimum payout, monthly payouts.">
  <link rel="canonical" href="https://legacyodyssey.com/affiliates">
  <meta property="og:title" content="Earn 35% recurring forever — Legacy Odyssey Affiliate Program">
  <meta property="og:description" content="For parenting creators, doulas, midwives, and baby-gear reviewers. Refer parents to Legacy Odyssey and earn 35% of every payment, recurring.">
  <meta property="og:url" content="https://legacyodyssey.com/affiliates">
  <meta name="robots" content="index, follow">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;1,400&family=Jost:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }
    :root {
      --bg: #faf7f2; --dark: #1a1510; --gold: #c8a96e; --gold-dark: #b08e4a;
      --text: #2c2416; --text-soft: #8a7e6b; --card: #f0e8dc; --border: #e0d5c4; --white: #fff;
    }
    html { scroll-behavior: smooth; }
    body { font-family: 'Jost', sans-serif; background: var(--bg); color: var(--text); line-height: 1.6; }
    .wrap { max-width: 1000px; margin: 0 auto; padding: 0 24px; }
    .serif { font-family: 'Cormorant Garamond', Georgia, serif; }

    /* Top bar */
    .topbar { background: var(--dark); padding: 18px 0; }
    .topbar .wrap { display: flex; align-items: center; justify-content: space-between; }
    .topbar a.brand { font-family: 'Cormorant Garamond', serif; font-size: 24px; font-weight: 600; color: var(--gold); text-decoration: none; letter-spacing: 0.5px; }
    .topbar a.back { color: #cbbfa9; text-decoration: none; font-size: 14px; }
    .topbar a.back:hover { color: var(--gold); }

    /* Hero */
    .hero { background: linear-gradient(160deg, #1a1510 0%, #2a2114 100%); color: #f5efe4; padding: 84px 0 76px; text-align: center; }
    .hero .eyebrow { color: var(--gold); letter-spacing: 3px; text-transform: uppercase; font-size: 12px; font-weight: 500; margin-bottom: 18px; }
    .hero h1 { font-family: 'Cormorant Garamond', serif; font-size: clamp(34px, 6vw, 56px); font-weight: 600; line-height: 1.1; margin-bottom: 20px; }
    .hero h1 em { color: var(--gold); font-style: italic; }
    .hero p.sub { font-size: clamp(16px, 2.2vw, 20px); color: #cdbfa6; max-width: 640px; margin: 0 auto 34px; }
    .btn { display: inline-block; background: var(--gold); color: #1a1510; text-decoration: none; font-weight: 600; font-size: 16px; padding: 16px 40px; border-radius: 10px; transition: background .2s, transform .2s; box-shadow: 0 8px 24px rgba(200,169,110,0.25); }
    .btn:hover { background: var(--gold-dark); transform: translateY(-2px); }
    .hero .approve-note { margin-top: 16px; font-size: 13px; color: #9d927e; }

    /* Sections */
    section.block { padding: 72px 0; }
    section.block.alt { background: var(--card); }
    .section-title { font-family: 'Cormorant Garamond', serif; font-size: clamp(28px, 4vw, 40px); font-weight: 600; text-align: center; margin-bottom: 14px; }
    .section-lead { text-align: center; color: var(--text-soft); max-width: 640px; margin: 0 auto 44px; font-size: 17px; }

    /* What is LO */
    .whatis { background: var(--white); border: 1px solid var(--border); border-radius: 16px; padding: 36px 40px; max-width: 760px; margin: 0 auto; box-shadow: 0 4px 20px rgba(0,0,0,0.04); }
    .whatis p { font-size: 18px; color: var(--text); }
    .whatis .demo { margin-top: 18px; font-size: 15px; }
    .whatis .demo a { color: var(--gold-dark); font-weight: 500; }

    /* Audience chips */
    .chips { display: flex; flex-wrap: wrap; gap: 12px; justify-content: center; max-width: 800px; margin: 0 auto; }
    .chip { background: var(--white); border: 1px solid var(--border); border-radius: 999px; padding: 10px 22px; font-size: 15px; color: var(--text); }

    /* How it works */
    .steps { display: grid; grid-template-columns: repeat(4, 1fr); gap: 22px; }
    .step { background: var(--white); border: 1px solid var(--border); border-radius: 14px; padding: 28px 22px; text-align: center; }
    .step .num { width: 42px; height: 42px; border-radius: 50%; background: var(--gold); color: #1a1510; font-weight: 600; font-size: 18px; display: flex; align-items: center; justify-content: center; margin: 0 auto 16px; font-family: 'Cormorant Garamond', serif; }
    .step h3 { font-family: 'Cormorant Garamond', serif; font-size: 20px; font-weight: 600; margin-bottom: 8px; }
    .step p { font-size: 14px; color: var(--text-soft); }
    @media (max-width: 760px) { .steps { grid-template-columns: 1fr 1fr; } }
    @media (max-width: 460px) { .steps { grid-template-columns: 1fr; } }

    /* Details box */
    .details { background: var(--white); border: 1px solid var(--border); border-radius: 16px; max-width: 720px; margin: 0 auto; overflow: hidden; }
    .details .row { display: flex; justify-content: space-between; padding: 16px 28px; border-bottom: 1px solid var(--border); font-size: 16px; }
    .details .row:last-child { border-bottom: none; }
    .details .row .k { color: var(--text-soft); }
    .details .row .v { font-weight: 600; color: var(--text); }

    /* Rules */
    .rules { max-width: 720px; margin: 0 auto; background: #fbf6ed; border: 1px solid var(--border); border-left: 4px solid var(--gold); border-radius: 12px; padding: 26px 30px; }
    .rules h3 { font-family: 'Cormorant Garamond', serif; font-size: 22px; margin-bottom: 12px; }
    .rules ul { list-style: none; }
    .rules li { padding: 6px 0 6px 24px; position: relative; color: var(--text); font-size: 15px; }
    .rules li::before { content: '✕'; position: absolute; left: 0; color: #b5532e; font-weight: 600; }
    .rules .tos { margin-top: 14px; font-size: 14px; color: var(--text-soft); }
    .rules .tos a { color: var(--gold-dark); }

    /* Final CTA */
    .final { background: linear-gradient(160deg, #1a1510 0%, #2a2114 100%); color: #f5efe4; text-align: center; padding: 80px 0; }
    .final h2 { font-family: 'Cormorant Garamond', serif; font-size: clamp(28px, 4vw, 42px); font-weight: 600; margin-bottom: 18px; }
    .final p { color: #cdbfa6; max-width: 560px; margin: 0 auto 30px; font-size: 17px; }

    footer.foot { background: #0f0b07; color: #8a7e6b; text-align: center; padding: 28px 0; font-size: 13px; }
    footer.foot a { color: var(--gold); text-decoration: none; }
  </style>
</head>
<body>

  <div class="topbar">
    <div class="wrap">
      <a href="/" class="brand">Legacy Odyssey</a>
      <a href="/" class="back">← Back to site</a>
    </div>
  </div>

  <!-- HERO -->
  <header class="hero">
    <div class="wrap">
      <div class="eyebrow">Friends of Legacy Odyssey</div>
      <h1>Earn <em>35% recurring forever</em><br>for every parent you refer.</h1>
      <p class="sub">Share something parents genuinely treasure — a digital baby book at their child's own .com domain — and earn 35% of every payment they make, for as long as they stay.</p>
      <a href="https://legacyodyssey.getrewardful.com/signup" target="_blank" rel="noopener" class="btn">Apply now →</a>
      <div class="approve-note">Approval typically within 48 hours.</div>
    </div>
  </header>

  <!-- WHAT IS LEGACY ODYSSEY (canonical description, verbatim) -->
  <section class="block">
    <div class="wrap">
      <h2 class="section-title">What you'll be sharing</h2>
      <div class="whatis">
        <p>Legacy Odyssey is a digital baby book — built as a real website at your child's own .com domain. Your baby gets their name as a website. Parents fill in milestones, photos, and letters through the iOS &amp; Android app. Family visits the book at your-childs-name.com, straight from any browser. Password-protected.</p>
        <p class="demo">See a live example: <a href="https://your-childs-name.com" target="_blank" rel="noopener">your-childs-name.com</a></p>
      </div>
    </div>
  </section>

  <!-- WHO IT'S FOR -->
  <section class="block alt">
    <div class="wrap">
      <h2 class="section-title">Who this is for</h2>
      <p class="section-lead">If your audience is expecting, new, or growing their family, this fits naturally into what you already share.</p>
      <div class="chips">
        <span class="chip">Parenting bloggers</span>
        <span class="chip">Doulas</span>
        <span class="chip">Baby gear reviewers</span>
        <span class="chip">Pregnancy influencers</span>
        <span class="chip">Midwives</span>
        <span class="chip">Lactation consultants</span>
        <span class="chip">Birth photographers</span>
        <span class="chip">Mom creators on Instagram &amp; TikTok</span>
      </div>
    </div>
  </section>

  <!-- HOW IT WORKS -->
  <section class="block">
    <div class="wrap">
      <h2 class="section-title">How it works</h2>
      <div class="steps">
        <div class="step"><div class="num">1</div><h3>Apply</h3><p>Tell us a little about you and your audience. It takes a couple of minutes.</p></div>
        <div class="step"><div class="num">2</div><h3>Get approved</h3><p>We review applications — usually within 48 hours — and send your unique link.</p></div>
        <div class="step"><div class="num">3</div><h3>Share your link</h3><p>Drop it in a post, a story, a newsletter, a blog review — wherever your people are.</p></div>
        <div class="step"><div class="num">4</div><h3>Earn 35%</h3><p>Get 35% of every payment your referrals make — recurring, as long as they're customers.</p></div>
      </div>
    </div>
  </section>

  <!-- PROGRAM DETAILS -->
  <section class="block alt">
    <div class="wrap">
      <h2 class="section-title">Program details</h2>
      <div class="details">
        <div class="row"><span class="k">Commission rate</span><span class="v">35% of every payment</span></div>
        <div class="row"><span class="k">Recurring</span><span class="v">Forever — for as long as your referral stays a customer</span></div>
        <div class="row"><span class="k">Cookie window</span><span class="v">90 days</span></div>
        <div class="row"><span class="k">Hold period</span><span class="v">60 days</span></div>
        <div class="row"><span class="k">Minimum payout</span><span class="v">$50</span></div>
        <div class="row"><span class="k">Attribution</span><span class="v">Last-touch</span></div>
        <div class="row"><span class="k">Payouts</span><span class="v">Monthly on the 15th, via PayPal or Wise</span></div>
      </div>
    </div>
  </section>

  <!-- WHAT'S NOT ALLOWED -->
  <section class="block">
    <div class="wrap">
      <div class="rules">
        <h3>A few things we don't allow</h3>
        <ul>
          <li>No coupon, cashback, or deal sites</li>
          <li>No paid search ads on Legacy Odyssey brand terms</li>
          <li>No incentivized or fake signups</li>
          <li>No spam — email, DM, or comment</li>
        </ul>
        <p class="tos">These keep the program fair for everyone. Full details are in the <a href="https://legacyodyssey.getrewardful.com/signup" target="_blank" rel="noopener">affiliate agreement</a> you'll accept when you apply.</p>
      </div>
    </div>
  </section>

  <!-- FINAL CTA -->
  <section class="final">
    <div class="wrap">
      <h2>Ready to earn 35% recurring?</h2>
      <p>Join the Friends of Legacy Odyssey program and start sharing a product parents are proud to give their child.</p>
      <a href="https://legacyodyssey.getrewardful.com/signup" target="_blank" rel="noopener" class="btn">Apply now →</a>
      <div class="approve-note" style="color:#9d927e;">Approval typically within 48 hours.</div>
    </div>
  </section>

  <footer class="foot">
    <div class="wrap">
      &copy; <span id="yr">2026</span> Legacy Odyssey · DOR Industries, LLC ·
      <a href="/">Home</a> · <a href="/privacy">Privacy</a> · <a href="/terms">Terms</a>
    </div>
  </footer>
  <script>document.getElementById('yr').textContent = new Date().getFullYear();</script>

</body>
</html>
