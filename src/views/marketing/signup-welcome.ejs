<!DOCTYPE html>
<html lang="en">
<head>
  <!-- Rewardful affiliate tracking (Friends of Legacy Odyssey) -->
  <script>(function(w,r){w._rwq=r;w[r]=w[r]||function(){(w[r].q=w[r].q||[]).push(arguments)}})(window,'rewardful');</script>
  <script async src='https://r.wdfl.co/rw.js' data-rewardful='0a0312'></script>

  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Welcome — Your website is being set up · Legacy Odyssey</title>
  <meta name="robots" content="noindex">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600&family=Jost:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>
    *, *::before, *::after { box-sizing: border-box; }
    body { margin: 0; font-family: 'Jost', system-ui, sans-serif; background: #1a1510; color: #faf7f2; min-height: 100vh; display: flex; align-items: center; justify-content: center; padding: 24px; }
    .ty { max-width: 580px; text-align: center; }
    .ty-mark { width: 72px; height: 72px; border-radius: 50%; border: 2px solid #c8a96e; display: flex; align-items: center; justify-content: center; margin: 0 auto 28px; }
    .ty-mark svg { width: 34px; height: 34px; color: #c8a96e; }
    .ty-eyebrow { font-size: 12px; letter-spacing: 4px; text-transform: uppercase; color: #c8a96e; margin-bottom: 16px; }
    .ty-h { font-family: 'Cormorant Garamond', serif; font-size: clamp(34px, 6vw, 52px); font-weight: 400; line-height: 1.1; margin: 0 0 20px; }
    .ty-h em { font-style: italic; color: #c8a96e; }
    .ty-sub { font-size: 17px; line-height: 1.65; color: rgba(250,247,242,0.8); margin: 0 0 16px; }
    .ty-steps { text-align: left; max-width: 420px; margin: 24px auto 32px; list-style: none; padding: 0; }
    .ty-steps li { display: flex; gap: 12px; padding: 8px 0; font-size: 15px; color: rgba(250,247,242,0.85); }
    .ty-steps svg { width: 18px; height: 18px; color: #c8a96e; flex-shrink: 0; margin-top: 3px; }
    .ty-cta { display: inline-block; background: #c8a96e; color: #1a1510; text-decoration: none; padding: 15px 34px; border-radius: 6px; font-size: 14px; font-weight: 700; letter-spacing: 0.5px; text-transform: uppercase; }
    .ty-cta:hover { background: #d4bb8a; }
    .ty-links { margin-top: 22px; font-size: 13px; }
    .ty-links a { color: rgba(250,247,242,0.6); text-decoration: underline; margin: 0 10px; }
  </style>
</head>
<body>
  <div class="ty">
    <div class="ty-mark"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg></div>
    <div class="ty-eyebrow">Payment received</div>
    <h1 class="ty-h">Welcome to <em>Legacy Odyssey.</em></h1>
    <p class="ty-sub">Thank you. We're setting everything up right now.</p>
    <ul class="ty-steps">
      <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>Check your email for a link to <strong>set your password</strong>.</li>
      <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>We're registering your <strong>.com domain</strong> — it can take a few minutes to go live.</li>
      <li><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>Then start adding photos and milestones from the app or the web editor.</li>
    </ul>
    <a href="/account" class="ty-cta">Go to my account</a>
    <div class="ty-links">
      <a href="/">Home</a>
      <a href="mailto:help@legacyodyssey.com">Need help?</a>
    </div>
  </div>
</body>
</html>
