<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Intentionally not indexed: this page is shared privately. -->
  <meta name="robots" content="noindex,nofollow">

  <title>Founder Invitation · Legacy Odyssey</title>
  <meta name="description" content="A private founder invitation to Legacy Odyssey — $29/year for as long as you keep your subscription.">

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400;1,500&family=Jost:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="/css/marketing.css">

  <style>
    /* ---------- BASE (fd- = "founder") ---------- */
    *, *::before, *::after { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    .fd-page { font-family: 'Jost', system-ui, sans-serif; background: #faf7f2; color: #2c2416; overflow-x: hidden; line-height: 1.65; margin: 0; }
    .fd-page p { margin: 0 0 16px; }

    /* ---------- HEADER NAV ---------- */
    .fd-nav {
      position: sticky; top: 0; height: 64px; z-index: 100;
      background: rgba(26, 21, 16, 0.95); backdrop-filter: blur(12px);
      display: flex; align-items: center; justify-content: space-between;
      padding: 0 28px;
      border-bottom: 1px solid rgba(200,169,110,0.18);
    }
    .fd-nav-logo {
      font-family: 'Cormorant Garamond', serif; font-size: 22px; font-weight: 500;
      color: #faf7f2; letter-spacing: 0.5px; text-decoration: none;
    }
    .fd-nav-logo span { color: #c8a96e; }
    .fd-nav-links {
      display: flex; align-items: center; gap: 26px;
      list-style: none; padding: 0; margin: 0;
    }
    .fd-nav-links a {
      color: rgba(250,247,242,0.78); text-decoration: none;
      font-size: 13px; font-weight: 500; letter-spacing: 0.3px;
      transition: color 0.2s;
    }
    .fd-nav-links a:hover { color: #c8a96e; }
    .fd-nav-cta {
      background: #c8a96e; color: #1a1510 !important; padding: 9px 20px;
      border-radius: 4px; font-size: 12px; font-weight: 700;
      letter-spacing: 0.5px; text-transform: uppercase;
      text-decoration: none; transition: background 0.2s;
      white-space: nowrap;
    }
    .fd-nav-cta:hover { background: #d4bb8a; }
    @media (max-width: 640px) {
      .fd-nav-link-md { display: none; }
      .fd-nav { padding: 0 16px; }
      .fd-nav-links { gap: 14px; }
    }

    /* ---------- HERO ---------- */
    .fd-hero {
      position: relative; padding: 96px 24px 88px;
      display: flex; align-items: center; justify-content: center;
      overflow: hidden;
    }
    .fd-hero-photo {
      position: absolute; inset: 0;
      background-image: url('https://images.unsplash.com/photo-1470116945706-e6bf5d5a53ca?w=1600&h=1000&fit=crop&crop=faces,center&auto=format&q=85');
      background-size: cover; background-position: center;
      filter: brightness(0.4);
    }
    .fd-hero-overlay {
      position: absolute; inset: 0;
      background: linear-gradient(180deg, rgba(26,21,16,0.4) 0%, rgba(26,21,16,0.6) 60%, rgba(26,21,16,0.92) 100%);
    }
    .fd-hero-content { position: relative; z-index: 2; max-width: 820px; text-align: center; color: #faf7f2; }
    .fd-hero-eyebrow {
      font-family: 'Jost', sans-serif; font-size: 12px; font-weight: 500;
      letter-spacing: 4px; text-transform: uppercase; color: #c8a96e;
      margin-bottom: 22px;
    }
    .fd-hero-headline {
      font-family: 'Cormorant Garamond', serif; font-size: clamp(40px, 6.5vw, 76px);
      font-weight: 400; line-height: 1.06; color: #faf7f2; margin: 0 0 24px;
    }
    .fd-hero-headline em { font-style: italic; color: #c8a96e; font-weight: 400; }
    .fd-hero-sub {
      font-family: 'Cormorant Garamond', serif;
      font-size: clamp(18px, 2.2vw, 22px); font-weight: 400; font-style: italic;
      line-height: 1.55; color: rgba(250,247,242,0.88);
      max-width: 600px; margin: 0 auto 32px;
    }
    .fd-hero-cta {
      display: inline-flex; align-items: center; gap: 10px;
      background: #c8a96e; color: #1a1510; padding: 16px 36px; border-radius: 6px;
      font-size: 14px; font-weight: 700; letter-spacing: 0.5px; text-transform: uppercase;
      text-decoration: none; transition: background 0.2s; border: none; cursor: pointer; font-family: inherit;
    }
    .fd-hero-cta:hover { background: #d4bb8a; }
    .fd-hero-cta-note { font-size: 13px; color: rgba(250,247,242,0.55); margin-top: 14px; }

    /* ---------- SECTION GENERICS ---------- */
    .fd-section { padding: 84px 24px; }
    .fd-section-inner { max-width: 720px; margin: 0 auto; }
    .fd-section-eyebrow {
      font-family: 'Jost', sans-serif; font-size: 12px; font-weight: 500;
      letter-spacing: 4px; text-transform: uppercase; color: #b08e4a;
      margin-bottom: 14px;
    }
    .fd-section-headline {
      font-family: 'Cormorant Garamond', serif; font-size: clamp(32px, 4vw, 48px);
      font-weight: 400; line-height: 1.15; color: #1a1510; margin: 0 0 20px;
    }
    .fd-section-headline em { font-style: italic; color: #b08e4a; }
    .fd-prose { font-size: 17px; line-height: 1.75; color: #2c2416; }
    .fd-prose p { margin: 0 0 18px; }
    .fd-prose em { font-style: italic; }
    .fd-prose strong { font-weight: 600; color: #1a1510; }
    .fd-prose a { color: #b08e4a; text-decoration: underline; text-decoration-color: rgba(176,142,74,0.4); text-underline-offset: 3px; }

    /* ---------- ABOUT / LETTER section (the "from Dan" note) ---------- */
    .fd-letter {
      background: #f5ede0;
    }
    .fd-letter-signoff {
      margin-top: 28px; font-family: 'Cormorant Garamond', serif;
      font-size: 18px; font-style: italic; color: #5e564a;
    }

    /* ---------- WHAT YOU GET ---------- */
    .fd-features { background: #1a1510; color: #faf7f2; }
    .fd-features .fd-section-headline { color: #faf7f2; }
    .fd-features .fd-section-headline em { color: #c8a96e; }
    .fd-features .fd-section-eyebrow { color: rgba(200,169,110,0.7); }
    .fd-feature-list { list-style: none; padding: 0; margin: 28px 0 0; }
    .fd-feature-list li {
      display: flex; gap: 14px; padding: 12px 0; font-size: 16px; color: rgba(250,247,242,0.9);
      line-height: 1.6; border-top: 1px solid rgba(200,169,110,0.12);
    }
    .fd-feature-list li:first-child { border-top: none; }
    .fd-feature-check {
      flex-shrink: 0; width: 22px; height: 22px; border-radius: 50%;
      background: #c8a96e; color: #1a1510; display: flex; align-items: center; justify-content: center;
      margin-top: 2px;
    }
    .fd-feature-check svg { width: 12px; height: 12px; }
    .fd-feature-list strong { color: #faf7f2; font-weight: 600; }

    /* ---------- FOUNDER COMMITMENT ---------- */
    .fd-commitment {
      background: #faf7f2;
    }
    .fd-commitment-card {
      border: 1px solid rgba(184,142,74,0.25); border-radius: 12px;
      padding: 36px 32px; background: #fff;
    }
    .fd-commitment-card h3 {
      font-family: 'Cormorant Garamond', serif; font-size: 28px; font-weight: 500;
      color: #1a1510; margin: 0 0 18px;
    }
    .fd-commitment-list { list-style: none; padding: 0; margin: 0; }
    .fd-commitment-list li {
      display: flex; gap: 14px; padding: 10px 0; font-size: 15.5px; color: #2c2416; line-height: 1.6;
    }
    .fd-commitment-bullet {
      flex-shrink: 0; width: 6px; height: 6px; border-radius: 50%;
      background: #c8a96e; margin-top: 11px;
    }

    /* ---------- PRICING + FORM ---------- */
    .fd-buy {
      background: linear-gradient(180deg, #1a1510 0%, #2c2416 100%);
      color: #faf7f2; padding: 96px 24px;
    }
    .fd-buy-inner { max-width: 540px; margin: 0 auto; text-align: center; }
    .fd-buy .fd-section-eyebrow { color: rgba(200,169,110,0.7); }
    .fd-buy .fd-section-headline { color: #faf7f2; }
    .fd-buy .fd-section-headline em { color: #c8a96e; }
    .fd-buy-price {
      display: flex; align-items: baseline; gap: 10px; justify-content: center;
      margin: 24px 0 6px;
    }
    .fd-buy-price-num {
      font-family: 'Cormorant Garamond', serif; font-size: 84px; font-weight: 300;
      color: #faf7f2; line-height: 1;
    }
    .fd-buy-price-period { font-size: 18px; color: rgba(250,247,242,0.65); }
    .fd-buy-renewal { font-size: 14px; color: rgba(250,247,242,0.55); margin-bottom: 32px; line-height: 1.5; }

    /* form */
    .fd-form { text-align: left; }
    .fd-form-row { margin-bottom: 14px; }
    .fd-form label {
      display: block; font-size: 11px; letter-spacing: 0.5px; text-transform: uppercase;
      color: rgba(250,247,242,0.65); font-weight: 500; margin-bottom: 6px;
    }
    .fd-form input, .fd-form textarea {
      width: 100%; padding: 12px 14px;
      background: rgba(250,247,242,0.06); border: 1px solid rgba(250,247,242,0.18); border-radius: 6px;
      color: #faf7f2; font-family: 'Jost', sans-serif; font-size: 14px;
    }
    .fd-form input::placeholder, .fd-form textarea::placeholder { color: rgba(250,247,242,0.35); }
    .fd-form input:focus, .fd-form textarea:focus { outline: none; border-color: #c8a96e; background: rgba(255,255,255,0.07); }
    .fd-form textarea { min-height: 100px; resize: vertical; font-family: inherit; line-height: 1.55; }
    .fd-form-domain {
      display: flex; gap: 8px; align-items: stretch;
    }
    .fd-form-domain input { flex: 1; }
    .fd-form-domain .fd-form-suffix {
      display: flex; align-items: center; color: rgba(250,247,242,0.5);
      font-size: 15px;
    }
    .fd-form-check-btn {
      padding: 12px 18px; background: rgba(200,169,110,0.18); color: #c8a96e;
      border: 1px solid rgba(200,169,110,0.5); border-radius: 6px;
      font-size: 13px; font-weight: 600; letter-spacing: 0.5px; text-transform: uppercase;
      cursor: pointer; transition: background 0.2s; white-space: nowrap;
    }
    .fd-form-check-btn:hover { background: rgba(200,169,110,0.28); }
    .fd-form-check-result {
      font-size: 13px; line-height: 1.5; margin: 8px 0 0;
      min-height: 18px;
    }
    .fd-form-check-result.ok { color: #8ec793; }
    .fd-form-check-result.bad { color: #e89696; }

    .fd-form-submit {
      width: 100%; padding: 16px; background: #c8a96e; color: #1a1510;
      border: none; border-radius: 6px; font-size: 14px; font-weight: 700;
      letter-spacing: 0.5px; text-transform: uppercase;
      cursor: pointer; transition: background 0.2s; margin-top: 8px;
    }
    .fd-form-submit:hover:not(:disabled) { background: #d4bb8a; }
    .fd-form-submit:disabled { opacity: 0.55; cursor: not-allowed; }
    .fd-form-error { color: #e89696; font-size: 13px; min-height: 16px; margin: 8px 0 0; text-align: center; }
    .fd-form-finepr { font-size: 11px; color: rgba(250,247,242,0.4); margin: 14px 0 0; text-align: center; line-height: 1.5; }

    /* ---------- FOOTER ---------- */
    .fd-footer {
      background: #0f0d0a; color: rgba(250,247,242,0.55);
      padding: 56px 24px 36px;
      border-top: 1px solid rgba(200,169,110,0.15);
    }
    .fd-footer-inner {
      max-width: 980px; margin: 0 auto;
      display: grid; grid-template-columns: 1fr auto; gap: 36px; align-items: start;
    }
    @media (max-width: 700px) { .fd-footer-inner { grid-template-columns: 1fr; gap: 28px; } }
    .fd-footer-brand {
      font-family: 'Cormorant Garamond', serif; font-size: 22px; font-weight: 500;
      color: #faf7f2; margin-bottom: 8px;
    }
    .fd-footer-brand span { color: #c8a96e; }
    .fd-footer-tag { font-size: 13px; color: rgba(250,247,242,0.5); line-height: 1.55; max-width: 380px; }
    .fd-footer-links {
      display: flex; flex-wrap: wrap; gap: 22px; align-items: center;
      list-style: none; padding: 0; margin: 0;
    }
    .fd-footer-links a {
      color: rgba(250,247,242,0.7); text-decoration: none;
      font-size: 13px; transition: color 0.2s;
    }
    .fd-footer-links a:hover { color: #c8a96e; }
    .fd-footer-bottom {
      max-width: 980px; margin: 32px auto 0; padding-top: 22px;
      border-top: 1px solid rgba(200,169,110,0.1);
      display: flex; justify-content: space-between; flex-wrap: wrap; gap: 16px;
      font-size: 12px; color: rgba(250,247,242,0.4);
    }
    .fd-footer-bottom a { color: rgba(250,247,242,0.55); text-decoration: none; }
    .fd-footer-bottom a:hover { color: #c8a96e; }
  </style>

  <!-- Google Analytics 4 (kept so I can see if friends visited / converted) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-LMJVX82M3Q"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());
    // Tag this page as the founder landing so it doesn't pollute the v1/v2 A/B test.
    gtag('set', { 'landing_variant': 'founder' });
    gtag('config', 'G-LMJVX82M3Q');
  </script>
</head>
<body class="fd-page">

  <!-- ====== HEADER NAV ====== -->
  <nav class="fd-nav">
    <a href="/" class="fd-nav-logo">Legacy <span>Odyssey</span></a>
    <ul class="fd-nav-links">
      <li class="fd-nav-link-md"><a href="https://www.your-childs-name.com" target="_blank" rel="noopener">See Demo</a></li>
      <li class="fd-nav-link-md"><a href="/account">Sign In</a></li>
      <li><a href="#buy" class="fd-nav-cta">Join as a Founder</a></li>
    </ul>
  </nav>

  <!-- ====== HERO ====== -->
  <section class="fd-hero">
    <div class="fd-hero-photo"></div>
    <div class="fd-hero-overlay"></div>
    <div class="fd-hero-content">
      <div class="fd-hero-eyebrow">A private invitation</div>
      <h1 class="fd-hero-headline">Founder Pricing<br>for <em>Legacy Odyssey.</em></h1>
      <p class="fd-hero-sub">If you're here, it's because I asked you. I'm building Legacy Odyssey, and I'd like you to be one of the first people to actually use it — at a price that's locked in for as long as you stay.</p>
      <a href="#buy" class="fd-hero-cta">Become a Founder — $29/year →</a>
      <div class="fd-hero-cta-note">$29/year for as long as you keep your subscription. Cancel anytime.</div>
    </div>
  </section>

  <!-- ====== LETTER FROM ME ====== -->
  <section class="fd-section fd-letter">
    <div class="fd-section-inner">
      <div class="fd-section-eyebrow">Hi, I'm Dan</div>
      <h2 class="fd-section-headline">A few notes<br><em>before you decide.</em></h2>
      <div class="fd-prose">
        <p>This is a real thing I'm running myself. When you email support, you're emailing me. I built it because I wanted my daughter to have a real .com — her name, her story — and not just another folder of photos on a phone.</p>
        <p>It's early. Most things work, but some corners are still rough. That's part of the reason this page exists — you're going to bump into things that aren't quite right, and I'd really love to hear about them.</p>
        <p>What I'm asking from you: <strong>sign up, use it for your baby's book, and tell me what's working and what isn't</strong>. After a couple weeks, send me a note — what felt obvious, what felt confusing, what's missing, what you'd want me to build next.</p>
        <p>In exchange: <strong>$29/year, locked in</strong>. Not "for the first year" — for as long as you keep your subscription active. And direct access to me by email or text, no support tickets, no scripts.</p>
        <p class="fd-letter-signoff">— Dan</p>
      </div>
    </div>
  </section>

  <!-- ====== WHAT YOU GET ====== -->
  <section class="fd-section fd-features">
    <div class="fd-section-inner">
      <div class="fd-section-eyebrow">What you get</div>
      <h2 class="fd-section-headline">A real <em>.com</em><br>and a beautiful baby book.</h2>
      <p class="fd-prose" style="color:rgba(250,247,242,0.85);">Same product as everyone else, just at the founder price.</p>
      <ul class="fd-feature-list">
        <li>
          <div class="fd-feature-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
          <span><strong>A real .com domain</strong> — registered in your child's name (or however you'd like), one-of-a-kind, yours.</span>
        </li>
        <li>
          <div class="fd-feature-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
          <span><strong>A beautifully designed digital baby book</strong> — birth story, month-by-month milestones, family recipes, letters.</span>
        </li>
        <li>
          <div class="fd-feature-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
          <span><strong>Letters &amp; sealed messages</strong> — write to your child today, choose when they unlock (their 18th birthday, their wedding day, whenever).</span>
        </li>
        <li>
          <div class="fd-feature-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
          <span><strong>Unlimited photos</strong> — no storage caps, no compression.</span>
        </li>
        <li>
          <div class="fd-feature-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
          <span><strong>iOS &amp; Android app</strong> — fill in the book on your phone at your own pace.</span>
        </li>
        <li>
          <div class="fd-feature-check"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg></div>
          <span><strong>Password-protected &amp; private</strong> — only the people you share the password with can see it.</span>
        </li>
      </ul>
      <p style="margin-top:32px;text-align:center;">
        <a href="https://www.your-childs-name.com" target="_blank" rel="noopener" style="display:inline-block;color:#c8a96e;font-size:14px;font-weight:600;letter-spacing:0.5px;text-transform:uppercase;text-decoration:none;border-bottom:1px solid rgba(200,169,110,0.4);padding-bottom:3px;">See a Live Demo Book →</a>
      </p>
    </div>
  </section>

  <!-- (Founder agreement section removed per Dan's review.) -->

  <!-- ====== BUY ====== -->
  <section class="fd-buy" id="buy">
    <div class="fd-buy-inner">
      <div class="fd-section-eyebrow">Join in</div>
      <h2 class="fd-section-headline">Become a <em>founder.</em></h2>
      <div class="fd-buy-price">
        <div class="fd-buy-price-num">$29</div>
        <div class="fd-buy-price-period">/ year</div>
      </div>
      <div class="fd-buy-renewal">Locked in for as long as you keep your subscription. Cancel anytime — no questions.</div>

      <form class="fd-form" id="fdForm" novalidate>
        <div class="fd-form-row">
          <label for="fdEmail">Your email</label>
          <input type="email" id="fdEmail" name="email" placeholder="you@example.com" required>
        </div>

        <div class="fd-form-row">
          <label for="fdDomain">Your child's .com</label>
          <div class="fd-form-domain">
            <input type="text" id="fdDomain" name="domain" placeholder="your-childs-name" autocomplete="off" spellcheck="false" required>
            <span class="fd-form-suffix">.com</span>
            <button type="button" class="fd-form-check-btn" id="fdCheckBtn">Check</button>
          </div>
          <p class="fd-form-check-result" id="fdCheckResult"></p>
        </div>

        <div class="fd-form-row">
          <label for="fdNote">Anything you'd like me to know? <span style="text-transform:none;color:rgba(250,247,242,0.4);">(Optional)</span></label>
          <textarea id="fdNote" name="founderNote" placeholder="A note, a question, or just hello — completely optional."></textarea>
        </div>

        <button type="submit" class="fd-form-submit" id="fdSubmit" disabled>Continue to checkout — $29/year</button>
        <p class="fd-form-error" id="fdError"></p>
        <p class="fd-form-finepr">You'll be sent to Stripe to complete payment. We never store your card on our side.</p>
      </form>
    </div>
  </section>

  <!-- ====== FOOTER ====== -->
  <footer class="fd-footer">
    <div class="fd-footer-inner">
      <div>
        <div class="fd-footer-brand">Legacy <span>Odyssey</span></div>
        <div class="fd-footer-tag">
          A digital baby book at your child's own .com.<br>
          Questions before you sign up? Email <a href="mailto:hello@legacyodyssey.com" style="color:#c8a96e;">hello@legacyodyssey.com</a>.
        </div>
      </div>
      <ul class="fd-footer-links">
        <li><a href="https://www.your-childs-name.com" target="_blank" rel="noopener">See Demo</a></li>
        <li><a href="/account">Sign In</a></li>
        <li><a href="/blog">Blog</a></li>
        <li><a href="/terms">Terms</a></li>
        <li><a href="/privacy">Privacy</a></li>
      </ul>
    </div>
    <div class="fd-footer-bottom">
      <div>&copy; 2026 Legacy Odyssey · DOR Industries · A private founder invitation</div>
      <div><a href="/">legacyodyssey.com</a></div>
    </div>
  </footer>

  <script>
    // ---------- Domain availability check ----------
    const domainInput = document.getElementById('fdDomain');
    const checkBtn = document.getElementById('fdCheckBtn');
    const checkResult = document.getElementById('fdCheckResult');
    const submitBtn = document.getElementById('fdSubmit');
    let confirmedDomain = null;

    function sanitize(v) {
      return (v || '').toString().toLowerCase().trim().replace(/\.com$/i, '').replace(/[^a-z0-9-]/g, '');
    }

    async function checkDomain() {
      const raw = sanitize(domainInput.value);
      if (!raw || raw.length < 2) {
        checkResult.className = 'fd-form-check-result bad';
        checkResult.textContent = 'Enter at least 2 characters.';
        return;
      }
      domainInput.value = raw;
      checkResult.className = 'fd-form-check-result';
      checkResult.textContent = 'Checking…';
      submitBtn.disabled = true;
      confirmedDomain = null;
      try {
        const res = await fetch('/api/domains/search?name=' + encodeURIComponent(raw));
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Domain check failed.');
        const com = (data.results || []).find(r => r.tld === 'com');
        if (com && com.available) {
          confirmedDomain = com.domain;
          checkResult.className = 'fd-form-check-result ok';
          checkResult.textContent = '✓ ' + com.domain + ' is available.';
          submitBtn.disabled = false;
        } else {
          checkResult.className = 'fd-form-check-result bad';
          checkResult.textContent = '✗ ' + raw + '.com is taken. Try a variation (middle name, family name, nickname).';
          submitBtn.disabled = true;
        }
      } catch (err) {
        checkResult.className = 'fd-form-check-result bad';
        checkResult.textContent = err.message || 'Couldn\'t check that name.';
        submitBtn.disabled = true;
      }
    }

    checkBtn.addEventListener('click', checkDomain);
    domainInput.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') { e.preventDefault(); checkDomain(); }
    });
    domainInput.addEventListener('input', () => {
      // Any edit invalidates the previous check.
      if (confirmedDomain && sanitize(domainInput.value) !== sanitize(confirmedDomain.replace(/\.com$/, ''))) {
        confirmedDomain = null;
        submitBtn.disabled = true;
        checkResult.className = 'fd-form-check-result';
        checkResult.textContent = '';
      }
    });

    // ---------- Form submit → Stripe checkout ----------
    document.getElementById('fdForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      const errorEl = document.getElementById('fdError');
      errorEl.textContent = '';

      const email = document.getElementById('fdEmail').value.trim();
      const founderNote = document.getElementById('fdNote').value.trim();
      if (!email) { errorEl.textContent = 'Please enter your email.'; return; }
      if (!confirmedDomain) { errorEl.textContent = 'Please check that your domain is available first.'; return; }

      submitBtn.disabled = true;
      submitBtn.textContent = 'Processing…';
      try {
        const res = await fetch('/api/stripe/create-founder-page-checkout', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ email, domain: confirmedDomain, founderNote }),
        });
        const data = await res.json();
        if (!res.ok) throw new Error(data.error || 'Something went wrong.');
        if (data.url) {
          window.location.href = data.url;
        } else {
          throw new Error('No checkout session URL returned.');
        }
      } catch (err) {
        errorEl.textContent = err.message;
        submitBtn.disabled = false;
        submitBtn.textContent = 'Continue to checkout — $29/year';
      }
    });
  </script>
</body>
</html>
