name = "legacy-odyssey-v3"
main = "src/index.tsx"
compatibility_date = "2026-04-28"
compatibility_flags = ["nodejs_compat"]

# Workers.dev subdomain (auto-issued by Cloudflare) for the dev URL.
# Production cutover happens later by attaching custom domains under [routes].
workers_dev = true

# Observability — surface uncaught errors and console.log to the dashboard.
[observability]
enabled = true

# Environment variables that aren't secrets (committable).
# Real secrets (service role, session secret, Stripe key) get set via:
#   echo "$VALUE" | npx wrangler secret put NAME
[vars]
APP_DOMAIN = "legacyodyssey.com"
NODE_ENV = "development"
SUPABASE_URL = "https://vesaydfwwdbbajydbzmq.supabase.co"
# SUPABASE_ANON_KEY is the public key — safe to commit per Supabase docs
SUPABASE_ANON_KEY = "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZlc2F5ZGZ3d2RiYmFqeWRiem1xIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzE3MDQzMTYsImV4cCI6MjA4NzI4MDMxNn0.KkyVjCwv1lYO1PE7k2-8C02tNC3rYw08vba-Oer0WxM"

# Stripe — price IDs are PUBLIC and safe to commit. Source of truth: CLAUDE.md.
# The actual STRIPE_SECRET_KEY (sk_live_...) is set as a Worker secret separately.
STRIPE_PRICE_MONTHLY = "price_1T3PPGJk2GIrL5uS3EqkKOLO"
STRIPE_PRICE_SETUP = "price_1T3PPxJk2GIrL5uSWnmW3eHQ"
STRIPE_PRICE_ANNUAL = "price_1TLojVJk2GIrL5uS0oQORYsr"
STRIPE_PRICE_ANNUAL_INTRO = "price_1TLojVJk2GIrL5uS0oQORYsr"
STRIPE_ANNUAL_INTRO_COUPON = "sX2lEPb6"
STRIPE_PRICE_ADDITIONAL_DOMAIN = "price_1TDVIAQzzNThrLYKNnMljEkp"

# Approximated.app — public config (cluster IP + target are not secrets).
# APPROXIMATED_API_KEY is set via wrangler secret put separately.
APPROXIMATED_CLUSTER_IP = "137.66.1.199"
APPROXIMATED_TARGET = "legacy-odyssey-production.up.railway.app"
APPROXIMATED_TARGET_PORT = "443"

[triggers]
# Domain order processor — once a minute, scan domain_orders rows in
# pending/registering/registered/vhosts_added state and advance them one
# step. This decouples Stripe-checkout webhook handling (must finish in
# ~30s) from the multi-minute Spaceship + Approximated provisioning.
crons = ["* * * * *"]
